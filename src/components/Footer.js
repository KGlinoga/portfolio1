import React from 'react';

export default function Footer() {
    return (
        <>
            <h5 className="panel-footer container-fluid text-center ">Let's work together! email: kglinoga@gmail.com </h5>
        </>
    );
};
// this page requires some bootstrap styling so itll stick to the bottom
// do github and linkedin links instead of the copy
// darker color
// icons instead of the text - link the icons instead of text 
// resume link at the top - link to PDF, publicly viewable
// add professional headshot to the About page (near Welcome)
// edit the copy to not say "following sections".   
// more about me as a person instead, since the navbar links are pretty straightforward. 